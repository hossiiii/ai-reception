# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here

# Google Calendar Configuration
# JSON string of service account credentials
GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-project","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"your-service-account@your-project.iam.gserviceaccount.com","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"https://www.googleapis.com/robot/v1/metadata/x509/your-service-account%40your-project.iam.gserviceaccount.com"}

# Comma-separated list of calendar IDs for meeting rooms
MEETING_ROOM_CALENDAR_IDS=calendar-id-1@group.calendar.google.com,calendar-id-2@group.calendar.google.com

# Slack Configuration
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Twilio Video Configuration
TWILIO_ACCOUNT_SID=your_account_sid
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_API_KEY=your_api_key
TWILIO_API_SECRET=your_api_secret
FRONTEND_URL=http://localhost:3001

# Application Configuration
# Set to "production" for production environment, "development" for development
ENVIRONMENT=development
DEBUG=true

# CORS Configuration
# For production, specify allowed origins (comma-separated)
# Leave empty or omit for development (will allow all origins when ENVIRONMENT=development)
# Example: ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://your-domain.com
ALLOWED_ORIGINS=

# Session Configuration
SESSION_TIMEOUT_MINUTES=30
MAX_CORRECTION_ATTEMPTS=3

# ================================
# Setup Instructions:
# ================================
# 1. Copy this file to .env
# 2. Fill in your actual API keys and configuration values
# 3. For Google Calendar:
#    - Create a service account in Google Cloud Console
#    - Download the JSON credentials file
#    - Copy the entire JSON content as a single line for GOOGLE_SERVICE_ACCOUNT_KEY
#    - Share your calendar(s) with the service account email
# 4. For Slack:
#    - Create a Slack app and add an Incoming Webhook
#    - Copy the webhook URL
# 5. For OpenAI:
#    - Get your API key from https://platform.openai.com/api-keys
# 6. For CORS Configuration:
#    - Development: Leave ALLOWED_ORIGINS empty (allows all origins)
#    - Production: Set ENVIRONMENT=production and list allowed origins in ALLOWED_ORIGINS

# ================================
# Environment-specific examples:
# ================================

# Development (allows all origins):
# ENVIRONMENT=development
# ALLOWED_ORIGINS=  # Can be empty or omitted

# Production (restricts to specific origins):
# ENVIRONMENT=production
# ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://your-domain.com,https://app.example.com
# DEBUG=false